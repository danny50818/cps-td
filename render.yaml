services:
  - type: web
    name: frontline-guardian
    runtime: node
    region: singapore
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: LEADERBOARD_GAME_ID
        value: frontline-guardian-v4
      - key: DATABASE_URL
        fromDatabase:
          name: frontline-guardian-db
          property: connectionString

databases:
  - name: frontline-guardian-db
    region: singapore
